import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  auth: {
    marginTop: 40,
    marginBottom: 150,
    width: 300, 
    height: 50.53,
    borderRadius: 38,
  }
})

export default styles